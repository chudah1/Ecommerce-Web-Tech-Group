//only rate a product if it is in your cart  

        // first check if the session is set
        //check for the user's cart and if the product is not inside alert a msgfmt_parse_message
        // else insert into the cart
        // check if the user has rated the product before